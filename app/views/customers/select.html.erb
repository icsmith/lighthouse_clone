select
<%= simple_form_for @customer do |f| %>
<%= f.association :caseworker, label_method: lambda { |c| "#{c.first_name} #{c.last_name}" }, value_method: :id %>
<%= f.submit %>
<% end %>
<%= link_to "Create a new caseworker", new_caseworker_path %>